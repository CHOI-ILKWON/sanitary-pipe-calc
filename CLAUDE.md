# sanitary-pipe-calc

위생배관(급수·급탕·오배수·통기) 관경 산정 웹앱. 건물 전체를 트리로 입력해
개별 화장실 → 층 → 입상관 → 메인 각 단계의 관경·유량·부하단위를 산정하고 계통도를 그린다.

> **이 저장소는 공개(public)다.** 가격·매출·인증 백엔드의 운영 정보 등
> 비공개 정보를 이 파일이나 저장소 안에 쓰지 말 것.

## 구조

| 파일 | 내용 |
|---|---|
| `index.html` | 앱 전체 (약 2,700행). 계산·UI·계통도·엑셀 내보내기가 한 파일에 있다 |
| `auth.js` | 공통 인증 모듈 (`window.MEPAuth`). 다른 앱에도 복사해 쓰는 파일 |
| `apps-script/` | 라이선스 백엔드 (Google Apps Script) |

빌드 도구·프레임워크·패키지 의존성이 **없다.** 바닐라 JS 단일 파일이 의도된 설계다
(현장 PC에서 파일 하나로 열려야 한다). 번들러나 npm 의존성을 도입하지 말 것.

## 설계 기준은 이 저장소에 없다

계산에 쓰이는 값(부하단위, 동시사용계수, 관경 구간표, 통기 기준 등)의 **근거는
별도 볼트에 있다**: `~/mep-standards` (개인 비공개 저장소, 옵시디언).

코드의 상수 옆에는 기준 ID 주석이 붙는다:

```js
// 기준: WS-003
function flowPipe(q){ ... }
```

**계산 로직·상수를 고칠 때는 반드시 해당 기준 노트를 함께 고친다.** 한쪽만 고치면
어느 쪽이 맞는지 알 수 없게 된다. 둘이 다르면 **볼트가 맞다.**

볼트가 이 컴퓨터에 없으면, 계산 기준을 바꾸기 전에 사용자에게 알릴 것.

주요 기준 ID:

| ID | 내용 | 코드 위치 |
|---|---|---|
| `WS-001` | 기구별 급수 순간유량·접속관경 | `FX` 상수 |
| `WS-002` | 급수·급탕 동시사용계수 | `SIM_N`/`SIM_FV`/`SIM_GEN` |
| `WS-003` | 급수관경 유량 구간표 | `flowPipe()` |
| `DR-001` | 기구배수부하단위(DFU)·기구배수관경 | `FX` 상수 |
| `DR-002` | 배수 동시사용률 | `sewSimult()`/`wstSimult()` |
| `DR-003` | 배수관경 DFU 구간표 | `sewPipe()`/`wstPipe()` |
| `DR-004` | 통기관경 | `ventPipe()` |

## 계산 로직의 원칙

**결정론적으로 유지한다.** 같은 입력에 항상 같은 출력이 나와야 하고, 계산 과정을
그대로 펼쳐 보일 수 있어야 한다. 발주처 검증 요청에 "AI가 산정했습니다"는 통하지 않는다.
추론·AI 는 바깥 껍질(입력 해석, 설명문 초안)에만 쓴다.

**동시사용계수 이중 적용 금지.** 각 단계(화장실/층/입상관/메인)는 항상 **원시 합계**를
모아 자기 기구수로 계수를 적용한다. 자식 단계에서 이미 계수가 곱해진 값에 다시 곱하면
안 된다 — 실제로 있었던 버그다(`1aba0d8`). `newAcc()`/`accAdd()`/`accResult()` 구조가
이걸 막기 위한 것이니 흐트러뜨리지 말 것.

**관경 역전 금지.** 상류 관경은 하류 최대 관경 이상이어야 한다(`Math.max` 로 강제).

## 코드 스타일

기존 코드에 맞춘다 — 압축형이 의도다.

```js
if(q<=6.7)  return 15;
if(q<=13.8) return 20;
```

- `if(` 처럼 붙여 쓰고, 짧은 분기는 한 줄에 둔다
- 주석은 한국어. 섹션 구분은 `// ── 제목 ────────`
- 세로 정렬을 맞춘 표 형태 상수를 선호한다 (`FX`, `SIM_*` 참조)

## 커밋

한국어. 기존 관례를 따른다:

```
Feat: 동시사용계수표(공조위생 기술데이터북) 적용 + 이중 적용 버그 수정
Fix: 샤워기(shw)를 배수관경 계산에서 제외
```

계산 기준을 바꾼 커밋에는 기준 ID를 본문에 적는다.

## 배포

`main` 에 push 하면 Cloudflare Pages 가 자동 배포한다. 별도 빌드 단계 없음.

`auth.js` 의 Apps Script 호출은 **POST + `Content-Type: text/plain`** 이다.
Apps Script 가 OPTIONS 프리플라이트를 처리하지 못해 `application/json` 으로 보내면
CORS 로 차단된다. 이 헤더를 바꾸지 말 것.
